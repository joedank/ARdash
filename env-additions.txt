# Vector embeddings configuration
# Add these to your .env file

# DeepSeek API is already configured with:
# DEEPSEEK_API_KEY=sk-aebbce11d90a4a5190d6be5ccf1ee504
# DEEPSEEK_BASE_URL=https://api.deepseek.com/v1

# Embedding model to use (DeepSeek's OpenAI-compatible embedding model)
# Should be a model supported by DeepSeek's API
EMBEDDING_MODEL=embedding-2

# Feature flag for enabling the v2 estimate wizard
VITE_USE_ESTIMATE_V2=true
